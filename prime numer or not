#include<stdio.h>
void main()
{
int i,num,cnt;
printf("\n enter the number ");
scanf("%d",&num);
i=1;
while(i<=num)
{
if(num%i==0)
cnt++;
i++;
}
if(cnt==2)
printf("\n the number is prime");
else
printf("\n the number is not a prime");
}
